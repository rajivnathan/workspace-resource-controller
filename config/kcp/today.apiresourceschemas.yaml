apiVersion: apis.kcp.dev/v1alpha1
kind: APIResourceSchema
metadata:
  creationTimestamp: null
  name: today.samplesvcs.appstudio.redhat.com
spec:
  group: appstudio.redhat.com
  names:
    kind: SampleSvc
    listKind: SampleSvcList
    plural: samplesvcs
    singular: samplesvc
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      description: SampleSvc is the Schema for the samplesvcs API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: SampleSvcSpec defines the desired state of SampleSvc
          properties:
            foo:
              description: Foo is an example field of SampleSvc. Edit samplesvc_types.go
                to remove/update
              type: string
          type: object
        status:
          description: SampleSvcStatus defines the observed state of SampleSvc
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}

---
